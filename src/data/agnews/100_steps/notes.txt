In this run, we used: n_train=15000, n_val=1000
# Configurations start here
DEVICE = torch.device("cuda" if torch.cuda.is_available() else "cpu")
MAMBA, ATTN = 0, 1
MIN_LAYERS, MAX_LAYERS = 4, 20 # Talvez meter menos que 3
NUM_CLASSES = 4
MUTATION_RATE = 0.10
CROSSOVER_RATE = 0.8 
POP_SIZE = 30
GENERATIONS = 100
ELITISM = 1
LEARNING_RATE = 4e-4
FINE_TUNE = True

BATCH_SIZE = 32
STEPS_1 = 50

- Also, each run took about ~8h and something in a single NVIDIA GeForce RTX 3080 gpu

This where also results before changing mutation, and min_layers in the first generation 